version: '3.5'

services:
  node:
    build: .
    image: NodeBBCN.azurecr.io/nodebb-cn
    restart: unless-stopped
    depends_on:
      - proxy
    expose:
      - 4567

  proxy:
    build: ./nginx
    restart: unless-stopped
    ports:
      - 80:80
    expose:
      - 80
